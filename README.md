# {pytools}: Assorted Python functionalities in R
## Set Builder:
```that_for_all()...we_have()``` and ```that_for_any()...we_have()```
## Generator / Iterator:

<ul> 
  <li> <code>Generator()</code> </li> 
  <li><code> yield_next()</code> </li> 
  <li><code> as_Generator()</code> </li> 
  <li><code>is_Generator()</code> </li>
</ul>
